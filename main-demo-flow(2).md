> 状态：草稿
> 受众：内部
> 负责人：Codex
> 更新：2026-05-02

# 唯一主 Demo 链路

## 主链路一句话

```text
企业先建立企业身份证和资料库，再把混乱需求丢进来，系统诊断岗位 Agent、生成工作台、输出交付物，并在反馈中持续变好。
```

## 比赛展示边界

只展示网页端：

```text
进入网站
-> 注册 / 登录
-> 创建企业空间
-> 企业名称 + 企业网址
-> 一键生成企业身份证
-> 上传 PPT / 公司介绍
-> 建立企业库 / 部门库
-> 上传岗位需求
-> 需求路由：复用已有 Agent / 新增 workflow / 新建岗位 Agent
-> 系统诊断岗位并展示核心指标
-> 生成七层岗位骨架
-> Agent 诞生：头像 / 名字 / 工号 / 岗位说明
-> 员工入职仪式页（AI 员工证明卡）
-> 点击“开始交办任务”
-> 用户确认
-> 生成 Agent
-> AI 员工总览页（查看已有员工 / 版本状态 / 入口）
-> 进入工作台
-> 输出 Word / Markdown 等交付物
-> 可选 API Key / Webhook 一键发送
-> 反馈生成 V2
-> 第二次更好
```

不主动展示本地工作包、私有化部署、云端同步、ERP 连接器。评委追问企业数据安全时，再用一句话说明未来可导出标准化 Agent 工作包到企业本地运行。

## 3 分钟演示流程

主链路必须完整覆盖：

```text
登录
-> 创建企业
-> 企业身份证
-> 企业库 / 部门库
-> 上传需求
-> 需求路由
-> 诊断 + 指标
-> 确认
-> 生成
-> 执行
-> 反馈
-> 第二次更好
```

## 企业用户视角：页面目标与问题映射

| 页面 | 页面目标 | 解决的核心问题 | 成功标志（用户行为） |
|---|---|---|---|
| 首页 / 登录 | 让企业用户 10 秒内理解产品价值并愿意进入 | 用户不知道这是“聊天工具”还是“岗位交付系统” | 点击“开始体验”或完成登录 |
| 创建企业空间 | 用最小输入建立企业上下文起点 | 企业首次使用配置成本高、容易中途退出 | 填完企业名称和网址并创建成功 |
| 企业身份证页 | 自动生成企业画像并让用户确认红线 | 系统不了解企业背景，后续输出容易跑偏 | 用户确认或补充行业、目标、风险规则 |
| 企业库 / 部门库页 | 建立资料分层和可用边界 | 资料混乱，Agent 不知道该读什么 | 至少上传/选择企业库和部门库资料各 1 份 |
| 需求输入页 | 接住真实混乱输入，降低表达门槛 | 用户说不清需求、不会写规范提示词 | 粘贴文本或上传需求材料并提交 |
| 需求路由页 | 判断复用/新增 workflow/新建岗位 Agent | 需求增加会导致 Agent 爆炸 | 用户看到明确路由结论和原因并继续 |
| 诊断与指标页 | 把岗位价值显性化、可量化 | 用户看不懂“为什么是这个岗位 Agent” | 用户理解推荐岗位与 3-4 个核心指标 |
| 七层骨架页 | 明确岗位边界与执行标准 | Agent 能力边界不清，结果不可控 | 用户确认岗位边界、红线和交付标准 |
| 员工入职仪式页（证明卡） | 把“配置完成”转换成“员工入职”心智 | 缺乏仪式感，用户不把 Agent 当生产力角色 | 点击“开始交办任务” |
| AI 员工总览页 | 查看已有员工、版本状态和入口 | 企业看不到全局资产，难管理多人协作 | 进入某个员工详情或工作台 |
| 员工工作台页 | 真正发任务并执行交付 | 只有方案没有可操作执行界面 | 完成一次任务运行并得到 V1 输出 |
| 交付结果页 | 下载/发送结果并展示业务价值 | 输出不能落地到真实工作流 | 用户下载结果或触发 API/Webhook |
| 反馈进化页 | 把主观反馈转为可复用规则 | 结果一次性、不会持续变好 | 提交反馈并看到 V2 改进 |

页面设计原则（企业用户视角）：

- 每一页只回答一个核心问题，不在同页塞多个决策。
- 每一页有唯一主按钮，避免用户犹豫。
- 每一步都要有“继续下一步”的明确收益提示。
- 关键状态可回看：企业身份证、员工卡、反馈版本必须可追溯。

## 现场必须打出来的三个瞬间

### 第一下：企业身份证

用户只填企业名称和网址，系统生成企业身份证，并允许上传 PPT / 公司介绍补充。

评委要感受到：

> 我们不是一次性生成 Agent，而是先建立企业上下文。

### 第二下：需求路由 + 诊断跳转 + 核心指标

上传岗位需求后，系统不是问用户“你想搭什么”，而是直接输出：

> 这个需求不需要新建 Agent，应该归入市场部 ToB 内容增长 Agent，并新增一个“技术资料多平台改写”workflow。

然后输出：

> 你需要的是一个 ToB 内容增长 Agent。

同时展示：

> 核心指标：内容产出量、多平台适配耗时、人工修改率、技术事实错误率。

评委要感受到：

> 企业老板丢进来一段抱怨，系统不但知道该生成什么，还知道什么时候不该生成新 Agent，并能把岗位价值指标化。

### 第三下：七层骨架一屏呈现

诊断完直接展示完整七层骨架：

```text
岗位边界
工作顺序
质量标准
质量红线
交付契约
工具目录
学习回路
```

这一屏回答：

> 什么才算一个合格的垂直 Agent？

### 第四下：Agent 诞生

七层骨架确认后，系统生成一个有头像、有名字、有工号、有岗位说明的 Agent。

示例：

> 你好，我是小薯，小红书内容运营专员。我负责周产 15 篇内容，互动率目标 5% 以上。我会参考企业库和市场部库，但不会编造数据，也不会未经确认自动发布。

评委要感受到：

> 用户不是拿到一份配置，而是“招到了一个 AI 员工”。

### 第五下：反馈闭环

用户反馈：

> 标题太营销。

系统写入规则，第二次输出明显变专业。最好做成左右对比，让评委一眼看到 Agent 在学习。

这一刻证明：

> 它不是一次性 prompt，而是能进化的岗位员工。

### Step 1：注册 / 登录 / 创建企业空间

演示动作：

- 进入网站
- 点击注册 / 登录
- 弹出“创建企业空间”卡片
- 输入企业名称和企业网址

评委应看到：

- 这是一个正式企业工作台，不是一次性页面
- 新企业进入门槛很低

### Step 2：一键生成企业身份证

演示动作：

- 点击“一键生成企业身份证”
- 页面展示企业行业、客户、产品、语气、业务目标、风险红线
- 上传 PPT / 公司介绍补充

评委应看到：

- 企业个性化不是重新训练模型
- 企业身份证会成为所有 Agent 的默认上下文

### Step 3：建立企业库 / 部门库

演示动作：

- 页面展示企业库、市场部库、当前任务资料
- 上传或勾选资料：
  - 企业库：公司介绍、品牌语气、通用红线
  - 市场部库：历史文章、平台风格、活动方案
  - 当前任务资料：会议记录 / PDF / 截图

评委应看到：

- 企业资料不是一个大文件夹，而是有层级和权限
- 企业库解决公司是谁，部门库解决岗位怎么干活

### Step 4：输入岗位需求

演示动作：

- 打开“创建岗位 Agent”
- 上传一段真实会议记录、PDF、邮件截图，或粘贴口述需求

评委应看到：

- 用户不用先懂 Agent
- 混乱输入可以被系统识别

### Step 5：需求路由与 AI 员工编制判断

系统输出：

- 当前企业已有的 AI 员工编制表
- 本次需求建议归属哪个岗位 Agent
- 是复用已有 Agent、新增 workflow / Skill，还是新建岗位 Agent
- 不新建新 Agent 的原因

演示推荐：

```text
本次需求归入：市场部 · ToB 内容增长 Agent
处理方式：新增“技术资料多平台改写”workflow
不新建原因：它仍属于内容增长岗位，不是独立岗位职责
```

评委应看到：

- 我们不是需求越多 Agent 越多
- 系统有企业 AI 员工编制治理能力
- 这个产品解决“什么时候不该生成新 Agent”

### Step 6：AI 做需求诊断 + 指标

系统输出：

- 企业类型
- 推荐岗位 Agent
- 痛点类型：运营窒息 / 增长焦虑 / 两者都有
- 可自动化环节
- 不适合自动化、需要人工复核的环节
- 核心指标：产出量、节省人时、人工修改率、错误率

评委应看到：

- 产品不只是执行工具，而有咨询判断
- Agent 的价值被指标化

### Step 7：生成 workflow

系统输出：

- Agent 名称
- 输入项
- 所需知识库/规则
- workflow steps
- 每一步使用的工具模块
- 结果格式

评委应看到：

- 业务语言已经被翻译成机器可执行结构

### Step 8：Agent 诞生

演示动作：

- 展示 Agent 诞生卡：
  - 头像
  - 姓名
  - 工号
  - 岗位
  - KPI
  - 不做什么
  - 开场白
- 停留 3 秒，让评委看清楚
- 点击“开始工作”

评委应看到：

- 骨架是底层，头像和自我介绍是用户能感知的产品体验
- 用户心理从“配置工具”变成“招了一个人”

### Step 9：员工入职仪式页

系统在生成 Agent 后先进入“AI 员工证明卡”页面，展示：

- 员工头像、姓名、工号、岗位
- 岗位边界与质量红线
- 今日可交办任务建议
- 唯一主按钮：开始交办任务

评委应看到：

- 这是“员工入职”而不是“参数配置完成”
- 用户心智从“我在搭工具”变成“我在给员工派任务”

### Step 10：生成 Agent 操作台

系统根据 workflow 动态生成：

- 文件上传区
- 文本输入区
- 已读取企业记忆状态
- 已读取企业库 / 市场部库状态
- 运行按钮
- 执行步骤进度
- 结果展示区
- 人工复核提示

评委应看到：

- 不是只输出一段方案，而是生成了一个用户能操作的工具
- 这个工具每次运行前会读取企业记忆

### Step 11：运行一个真实任务

P0 推荐场景二选一：

1. 内容生产 Agent
   - 输入：英文技术文档 / 产品资料
   - 输出：公众号、知乎、LinkedIn、小红书等多平台内容草稿

2. 尺码表整理 Agent
   - 输入：混乱 Excel / 图片表格 / 文本名单
   - 输出：标准尺码表、异常项、缺失字段

评委应看到：

- 至少一条核心链路真实运行
- 输出结果不是 mock

### Step 12：输出交付物和商业价值解释

系统展示：

- Word / Markdown / Excel 等下载入口
- API Key / Webhook 一键发送入口
- 原本人工耗时
- Agent 执行耗时
- 节省人时
- 仍需人工确认的风险点
- 可保存为模板的场景

评委应看到：

- 这不只是黑客松 demo，而能回到咨询交付和商业化
- 先下载复制，后续可通过 API 打通变成一键发送

### Step 13：采集反馈，让 Agent 越用越好

用户动作：

- 确认结果可用
- 修改错误字段 / 不合适文案
- 给执行步骤评分
- 标记失败原因或需人工处理的环节

系统沉淀：

- 本次需求归属哪个场景模板
- 哪个步骤可靠，哪个步骤需要人工复核
- 哪些字段 / 规则需要下次优先检查
- 本次节省的人时、发现的异常和复用价值

评委应看到：

- 产品不是一次性生成器，而是一个会积累企业经验的工作底座

### Step 14：第二次生成更好

演示动作：

- 用户反馈：“标题太营销，不够专业”
- 系统写入反馈库：`标题应偏技术可信感，避免夸张营销词`
- 再次运行同一内容任务

评委应看到：

- 第二版标题和摘要明显更专业
- 系统不是口头说“越用越好”，而是把用户修改变成了下一次生成规则

### Step 14：保存版本与企业记忆

演示动作：

- 将第二版保存为 `V2`
- 展示本次新增企业记忆：
  - 标题偏好：偏技术可信，不要夸张营销词
  - 术语规则：关键英文术语保留
  - 风格规则：ToB 技术内容优先场景和业务价值
- 点击“追加更新企业记忆”，页面显示 `Memory v2 已更新`
- 页面显示反馈写入位置：企业库 / 市场部库 / Agent 学习回路

评委应看到：

- 个性化不是训练模型，而是企业画像、知识库、模板库、长期记忆和反馈规则
- 下次运行时，Agent 会先读取这些企业记忆
- 企业记忆可以持续上传、保存、更新，不是一次性配置

## P0 验收标准

- 能展示登录 / 注册 / 创建企业空间
- 能展示企业名称 + 企业网址
- 能展示一键生成企业身份证
- 能展示企业库 / 市场部库 / 当前任务资料
- 输入需求后能稳定生成结构化诊断
- 能展示企业 AI 员工编制表
- 能展示需求路由结果：复用已有 Agent / 新增 workflow / 新建岗位 Agent
- 能演示一次“不新建 Agent，而是归入现有岗位 Agent”
- 能展示核心指标卡
- 能生成合法 workflow JSON
- 能展示 Agent 诞生卡：头像、名字、工号、岗位说明、开场白
- 前端能把 workflow 展示为流程卡片
- 能根据 workflow 进入一个 Agent 操作台
- 至少一个 Agent 能真实处理文件并输出结果
- 能记录一次用户反馈：确认、修改、评分或异常标记
- 能展示 V1 / V2 版本对比，至少保存一条企业记忆或反馈规则
- 能展示 Word / Markdown 下载入口和 API Key / Webhook 配置入口
- 能展示企业记忆的上传入口、保存按钮、更新按钮和记忆版本
- 刷新 / 网络失败有兜底数据

## 失败兜底

- LLM 调用失败：使用预置 workflow 示例
- 文件解析失败：使用内置样例文件
- 外网失败：本地版 + 录屏版
- 时间不够：优先保主链路，不做多场景
